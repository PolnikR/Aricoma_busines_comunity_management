node_modules
dist
.git
.github
.vscode
.idea
docs
coverage
*.log
*.local
.env
.env.*
# Vite inlines VITE_* at build time, so the production env file must reach the
# build context. It holds no secrets (public Keycloak client config).
!.env.production
